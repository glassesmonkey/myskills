interface:
  display_name: "Web Search"
  short_description: "Exa-first web search with Tavily and DDG fallback"
  default_prompt: "Use $web-search to search the web, prefer Exa, then Tavily, and fall back to DuckDuckGo when needed. For deep research, cross-check all three providers."
