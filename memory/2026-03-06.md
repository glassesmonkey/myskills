# 2026-03-06

- 超哥要求把我的名字固定为“小威”，已同步到身份与长期记忆。
- 记录运行环境：OpenClaw 当前运行在 WSL2 环境下的 Ubuntu 中。
- Codex notify hook 已实测跑通：Codex 会把事件 JSON 作为 `notify.py` 的第一个命令行参数传入；`agent-turn-complete` 事件可通过 `openclaw system event --mode now` 立即唤醒主会话。
- 已建立 GitHub 迁移备份仓库：`Rice-PurityTest/openclaw_backup_i732g_xiaowei`，并写好可重复执行脚本 `scripts/push-openclaw-backup.sh`。
- 已新增 OpenClaw cron：每 6 小时执行一次迁移备份同步（成功静默，失败告警）。
- 已安装 `gh` CLI（Ubuntu apt 包，版本 2.4.0），用于后续验证 GitHub 仓库创建权限与管理仓库。
