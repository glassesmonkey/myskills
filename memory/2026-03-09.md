- 08:16 已获超哥许可，为 ballkorokoro 日语 research 在 WSL 内启用浏览器链路；将安装 Playwright Chromium runtime / agent-browser 所需运行时，并同步优化 web-research skill 的 fallback 说明。

- 20:30 左右，为解决“长任务空转/会话活着但不推进”的 recurring 问题，新增并补齐 skill：`skills/task-execution-tracker/`。核心规则：大任务先建 task state file；one worker one role；每轮必须有 done definition + handoff；连续两次无实质变更判定 stalled，必须升级处理。
- 20:20 左右，新增 skill：`skills/multi-acp-collaboration/`，用于在一个 TG 群作为 control chat 的前提下调度多个 ACP session。当前结论：ACP 比 tmux 更适合“会话状态感知/steer/cancel/close”，但“session done ≠ task done”，最终完成判定仍需结合 handoff、git diff、build/test。
- 关于 OpenClaw 路由的关键认知：Telegram 群本身对应一个 group sessionKey（类似 `agent:main:telegram:group:<chatId>`）；可以把大任务拆到多个独立 subagent/ACP session 里执行，并把结果汇总回同一个 TG 群，但 Telegram 群不是 thread-bound 多任务界面，所以必须依赖 label、状态文件和结构化摘要，避免消息流混乱。
- 20:20~20:30，首次按 `task-execution-tracker` 实际执行 ballkorokoro 日语重写任务：先建 `ballkorokoro.com/tasks/ballkorokoro-ja-rewrite.md`，再按“首页 → 3 个 detail pages → ja gamelist → build”的顺序推进，避免再陷入只讨论方法论不落地的空转。
- 20:30 后，已在 `ballkorokoro.com` 仓库完成 JP 文案重写并通过 build：更新 `pages/index.tsx`、`pages/game-list/ballkorokoro1.tsx`、`pages/game-list/newballkorokoro1.tsx`、`pages/game-list/newballkorokoro4.tsx`、`public/locales/ja/common.json`、`public/locales/ja/gamelist.json`，并成功执行 `npm run build`（含 sitemap 生成）。提交 commit：`0d71f7d`，message：`feat: rewrite JP ball rolling copy`。
- 22:33~23:25 对 `sessions_spawn` 做了专门排查：当前 Telegram 群会话里，模型显式看到的工具列表一度只有 `sessions_list` / `sessions_history` / `sessions_send`，看起来像没有 `sessions_spawn`；但实际 subagent spawn 测试成功，说明“模型可见工具表”与“运行时真实可执行能力”不是同一层。
- 关键认知更新：`/subagents spawn` 是独立的用户命令链路，不等同于模型 first-class tool `sessions_spawn`。因此“命令能 spawn”并不自动说明当前模型工具表里一定有 `sessions_spawn`，反之亦然。
- 源码排查结果：未发现 Telegram 群会话对 `sessions_spawn` 做专门特判过滤；group 工具策略走的是通用逻辑（`resolveGroupToolPolicy` / `resolveChannelGroupToolsPolicy`），而当前 Telegram 群配置也没有设置 `tools` / `toolsBySender` deny，更像是工具展示层或 tool manifest 与运行时能力存在不一致。
- 本机当前 shell 里 `openclaw` 不在 PATH；排查 CLI 时需直接用绝对路径 `/home/gc/.nvm/versions/node/v22.22.0/bin/openclaw`。
